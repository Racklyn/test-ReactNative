const DATABASE_NAME = '@mygps_tracking'

const SAVED_PACKAGES = `${DATABASE_NAME}:saved_packages`
const CURRENT_PACKAGE = `${DATABASE_NAME}:current_package`

export {
    SAVED_PACKAGES,
    CURRENT_PACKAGE
}